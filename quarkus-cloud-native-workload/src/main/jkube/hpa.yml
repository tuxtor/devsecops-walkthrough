apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ${project.artifactId}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ${project.artifactId}
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 25
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 10 # Delay downscaling by 1 min
    scaleUp:
      stabilizationWindowSeconds: 10 # Delay upscaling by 1 min
